# TradeReporter deployment config
# Edit these values. Other files get them via deploy/apply-config.sh

SERVER_IP=205.198.68.58
DOMAIN_NAME=opticanvas.com

# nginx | direct (port 3000/8000)
# nginx = serve on 80/443, API at /api (same-origin)
DEPLOY_MODE=nginx

# Email for Let's Encrypt expiry notices (optional - leave unset to skip)
# CERTBOT_EMAIL=you@example.com
