{
  "name": "str",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,green \"npm run dev -w apps/web\" \"echo 'API not yet scaffolded'\"",
    "dev:web": "npm run dev -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "react-grid-layout": "^2.2.2"
  }
}
