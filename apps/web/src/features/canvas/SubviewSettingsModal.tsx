import { SubviewEditorModal } from '@/features/subviews/subview-editor';

/** Wrapper: SubviewEditorModal is the Phase 5 Subview Editor (JSON+Python). */
export function SubviewSettingsModal() {
  return <SubviewEditorModal />;
}
